const ConceptsHeader = (props) => {
  return (
    <header>
      <img src={props.img} alt="Medal badge with a star" />
      <h1>Conceitos-chave do React</h1>
      <p>Principais conceitos do React que você deve conhecer:</p>
    </header>
  );
};
export default ConceptsHeader;
